plugins {
    id 'onefreelance.shared-jar'
}

group "org.grisbi.onefreelance"
apply from: '../version.gradle'
version = loadVersion().getVersion()

dependencies {

}

sonar {
    properties {
        property "sonar.projectKey", "GillesAndrieu_onefreelance-security"
        property "sonar.projectName", "OneFreelance | security"
    }
}